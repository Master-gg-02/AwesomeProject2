{
  "name": "AwesomeProject",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "node node_modules/react-native/local-cli/cli.js start",
    "test": "jest",
    "bound-android": "rm -rf out/android/*  && rm -rf out/android.zip  && react-native bundle --platform android --dev false --entry-file index.js --reset-cache --bundle-output ./out/android/RouterAddPanel.android.bundle --assets-dest ./out/android && cd out/ && zip -q -r android.zip android  \n"
  },
  "dependencies": {
    "react": "16.8.3",
    "react-native": "0.59.10"
  },
  "devDependencies": {
    "@babel/core": "^7.17.10",
    "@babel/runtime": "^7.17.9",
    "babel-jest": "^28.1.0",
    "jest": "^28.1.0",
    "metro-react-native-babel-preset": "^0.70.3",
    "react-test-renderer": "16.8.3"
  },
  "jest": {
    "preset": "react-native"
  }
}
